import { GifsResult } from '@giphy/js-fetch-api';
import { IGif, IUser } from '@giphy/js-types';
import { Component, h, JSX } from 'preact';
import { debounce } from 'throttle-debounce';
import { EventProps } from './gif';
declare type Props = {
    className?: string;
    width: number;
    user?: Partial<IUser>;
    columns: number;
    gutter: number;
    fetchGifs: (offset: number) => Promise<GifsResult>;
    onGifsFetched?: (gifs: IGif[]) => void;
    onGifsFetchError?: (e: Error) => void;
    noResultsMessage?: string | JSX.Element;
    hideAttribution?: boolean;
    noLink?: boolean;
    tabIndex?: number;
    borderRadius?: number;
} & EventProps;
declare type State = {
    gifWidth: number;
    isFetching: boolean;
    isError: boolean;
    isDoneFetching: boolean;
    numberOfGifs: number;
    gifs: IGif[];
    isLoaderVisible: boolean;
};
declare class Grid extends Component<Props, State> {
    static className: string;
    static loaderClassName: string;
    static readonly defaultProps: Readonly<{
        gutter: number;
        user: {};
    }>;
    static fetchDebounce: number;
    readonly state: Readonly<{
        isFetching: boolean;
        isError: boolean;
        numberOfGifs: number;
        gifWidth: number;
        gifs: IGif[];
        isLoaderVisible: boolean;
        isDoneFetching: boolean;
    }>;
    bricks?: any;
    el: HTMLElement | null;
    paginator: () => Promise<IGif[]>;
    static getDerivedStateFromProps({ columns, gutter, width }: Readonly<Props>, prevState: Readonly<State>): {
        gifWidth: number;
    } | {
        gifWidth?: undefined;
    };
    setBricks(): void;
    componentDidMount(): void;
    componentDidUpdate(prevProps: Props, prevState: State): void;
    onLoaderVisible: (isVisible: boolean) => void;
    onFetch: debounce<() => Promise<void>>;
    render({ onGifVisible, onGifRightClick, className, onGifClick, onGifHover, onGifKeyPress, onGifSeen, user, noResultsMessage, hideAttribution, noLink, tabIndex, borderRadius, }: Props, { gifWidth, gifs, isError, isDoneFetching }: State): h.JSX.Element;
}
export default Grid;
