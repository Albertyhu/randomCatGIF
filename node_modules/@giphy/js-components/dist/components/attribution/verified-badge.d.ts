import { h } from 'preact';
declare type Props = {
    className?: string;
    size?: number;
    fill?: string;
};
declare const VerifiedBadge: {
    ({ className, size, fill }: Props): h.JSX.Element;
    className: string;
};
export default VerifiedBadge;
